# Findings 2026-09-17 (UTC)

All timestamps in this file, and the filename, are UTC. Count wins by CONTRACT ADDRESS, not by entry: one token pings once per horizon and each ping says which.

## 19:28:30 UTC · liveness · detector.drift

detector.drift last fired 62.2h ago, over its 12h threshold

```
Expected: the template fingerprint was re-measured. It has fired 488 times before, so the path works and something stopped it.
```

## 19:28:30 UTC · liveness · fieldguard.check

fieldguard.check last fired 62.3h ago, over its 12h threshold

```
Expected: produced fields were diffed against stored fields. It has fired 93 times before, so the path works and something stopped it.
```

## 19:28:30 UTC · liveness · milestone.mcap

milestone.mcap last fired 62.3h ago, over its 48h threshold

```
Expected: a token crossed 100k/200k/1m/5m and was claimed LIVE. It has fired 1114 times before, so the path works and something stopped it.
```

## 19:28:31 UTC · liveness · milestone.realizable

milestone.realizable last fired 64.2h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 19:28:31 UTC · liveness · news.freshness

news.freshness last fired 62.2h ago, over its 12h threshold

```
Expected: per-outlet news staleness was checked. It has fired 493 times before, so the path works and something stopped it.
```

## 19:28:32 UTC · liveness · outcome.recorded

outcome.recorded last fired 62.2h ago, over its 12h threshold

```
Expected: an outcome row was written at some horizon. It has fired 28415 times before, so the path works and something stopped it.
```

## 19:28:32 UTC · liveness · paper.open

paper.open last fired 62.4h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 247 times before, so the path works and something stopped it.
```

## 19:28:33 UTC · liveness · paper.sweep

paper.sweep last fired 62.3h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 19:28:33 UTC · liveness · scan.observations

scan.observations last fired 62.3h ago, over its 12h threshold

```
Expected: the scanner journalled at least one observation. It has fired 7126 times before, so the path works and something stopped it.
```

## 19:28:34 UTC · liveness · watchlist.sweep

watchlist.sweep last fired 62.3h ago, over its 12h threshold

```
Expected: the approach band was re-checked. It has fired 38 times before, so the path works and something stopped it.
```

## 19:30:25 UTC · selftest · k

summary

