# Crypto daily, 2026-09-22

Window: the 24h ending about 11:45Z on 2026-09-22. Both collectors (desktop
working tree plus the runner's commits on origin). Methods pre-committed in
`analysis/daily_2026-09-22/PRECOMMIT.md` before either measurement ran.

## Correction first

Yesterday EMBER (`FLCr9vGMTkbDcRCoirP5Hx8gB7TW1Azt3pkw3qp2HTsh`) was given as
the one milestone with real depth: 3.97x at 6h on $292k of quote-side depth,
both price sources agreeing. Its pool has since been drained. The recorded
pumpswap pool (`AVW2pYeHAWUgSGaz1F46pGVXVZ8d4DWerDfpa3NKWS43`) holds $0, and
all three EMBER pools together hold $2.64. Treat it as dead.

## Most surprising: yesterday's wins did not last a day

15 contracts logged a gate-passing 2x or better in the 24h. Re-checked live,
one Dexscreener call each:

| now | contracts |
|---|---|
| every pool under $100 | EMBER `FLCr9vGM…`, USDCAT `GPqoXbff7NPyvbngpsUz7mP1byZq7Xfh94E8Gu56HR6T`, TSLA `3gAXa6khcC6G7tq6XBnonsiS7GQtq3auKnXCvdZvQtfJ`, OWL `671dNhKr12xRoPmfkevzG1daqi4c9KMAA9J83ExyMWJ7`, X7 `BsE3aa5FdE6FVEkWvA8ShAGpzZasEgJbfSqRC4LmErTK` |
| down 96% to 99.97%, about $2k left | ChatGPT `MjimYVjN…`, MrBeast `HGNPr1zt…`, tradecat `EHY56TBN…`, Vortex `5vg9KLxC…`, UOTF `k4WcTJwc…` |
| still trading | STONKBROS `9KmeDWVt7TtrEZT2567kxeDkDZxd3cDoTZ1ZrLow9soG` ($32.7k), VSOF `54c53NaoMDLcxv9ECjN35GLJjX4JDbpbwpSiFwbspump` ($632k, a fake-fund name), X7 `CdhZy8wrRxNxoV8HtoByXKN7mvS46avtuZ1b39tKfx7` ($279k), CATEWALK `BLSuVTxKYDL4vm4XmG3oEJfsSGfJZF3cgy98ri68pump` ($91k), GO `HXQ66zSRqynwJQ6vYEYa85qGY9C2Ycz8rnYQHgApn391` ($84k) |

Drained pools keep printing numbers on Dexscreener: TSLA shows $60.6M of 24h
volume on $0.06 of liquidity, EMBER $163M on $0. Frank has no realized P&L,
and this is why a paper win is not a track record.

## 1. Is the data accurate?

321 recorded multiples of 2x or more on 195 contracts, plus a seeded random 60
contracts: 272 Dexscreener calls in total, one address per call, never batched.

- Still checkable (2x+): 158 of 176 rows agree within 2x (90%); 11
  overstated, 7 understated. Random sample: 81 of 82 agree.
- 145 of 321 rows (45%) can no longer be checked at all: Dexscreener lists no
  pools for 81 of those contracts, and 16 rows' recorded pool is gone.
- Worst: UOTF `k4WcTJwcK8x11E7RFhXsYsdiWZWwfo6FzKEmcCPpump` logged 2.01x at 6h
  (gate-passing), now 0.0003x on the same pool. Its pools agree with each
  other, so this is a collapse after the check, not bad data.
- The Supabase mirror matches git exactly: 2,134 observations on 1,882
  contracts in the 24h.

## 2. Is it useful?

- Coverage: scan ran 24 of 24 hours. Outcome, graduation and universe stages
  23 of 24 (see the 3,322s thread below).
- 2,134 observations on 1,882 contracts; 126 cleared the filter; 91 graded 100.
- We saw 9.0% [7.5, 10.8] of pump.fun graduations (104 of 1,152). Launch-level
  coverage was last measured at 1.26% on 2026-09-18 and not re-measured today.
- Alerts: 28 launch alerts covering 126 contracts. 12 of those contracts went
  on to a gate-passing 2x, 7 to a liquidity-gated $1M+ crossing. 3 of the 15
  gate-passing 2x contracts were never alerted. Of 361 findings, 228 (63%)
  were one stale line repeating: a 13-day-old self-test beat.
- **Miss count, our journal: 0 of 11** liquidity-gated $1M/$5M crossings
  went unalerted.
- **Miss count, the market: 95 of 105** tokens that newly reached $1M with a
  real $100 round trip were never in our journal at all (90.5% [83.4, 94.7]).
  Of those, 50 claim their cap on under 1% backing and 42 are already under
  $10k; 12 still look real by our checks, 4 of them fake-fund names.
- **Correction to the brief:** phantoms are not a third. 64 of 75 $1M/$5M
  crossing rows today (85%) failed the realizability check at crossing.

## 3. Experiment: does score predict survival independent of peak?

**NOT SUPPORTED** under the pre-committed rule. 15,034 contracts, 4,392 with
a known 24h label (survived = alive with at least $1,000 quote-side depth).
Filter-passing tokens survive far more often, 12.6% [9.8, 15.9] vs 1.9%
[1.5, 2.3]. Pooled odds ratio 2.75 [1.94, 3.89]. But among tokens that had
already doubled by 6h it reverses: 4.9% [2.1, 11.0] (n=102) vs 26.4% [19.9,
34.0] (n=148). The test is also confounded: 126 of 130 survivors were AMM
pools, and unknown labels are 49% vs 72% by arm. Sharpened in `QUESTIONS.md`:
the next test restricts to AMM pools.

## 4. Strategy candidates

No new strategy.

## 5. Monetization

- Track record: still dead. No realized P&L, RULE_V3 failed its own test,
  and today's paper wins evaporated within hours.
- Dexscreener API terms re-read today, last updated 2023-08-18 and unchanged:
  commercial use allowed; making the data available to third parties
  prohibited; no product that competes with theirs.
- Licensing the dataset: weaker today. The prices are theirs, and 45% of
  yesterday's 2x claims cannot be re-verified a day later. Only a chain-only
  dataset (graduation ledger, round-trip verdicts) would be ours to license.
- Newly more viable: a "what is fake" tool or research note built on our own
  chain reads (drained pools, caps on under 1% backing, 54% of launches reusing
  a ticker). It needs no track record. Still a lawyer's call before selling.

## Threads

- **`name`: confirmed and fixed.** 0 of 2,924 observation rows over two days
  stored the full name. `scanner.py` now keeps it as `token_name` and
  `journal.record()` persists it; `test_whitelist.py` checks it lands with
  non-ASCII intact. The Supabase RPC reads named keys only, so the mirror is
  unaffected (checked against the function's definition). Verified on real
  output: the scheduled pass at 12:04Z wrote 81 rows, 81 with `token_name`,
  non-ASCII intact, 56 differing from the ticker. Nothing reads it yet.
- **3,322s pass: killed.** That number was the gap until the next pass found
  a dead market stage's marker (started 18:08:19Z, noticed 19:03:41Z), not a
  runtime. The stage was launched normally by the scheduled task. The real
  cost: nothing after it ran that hour. Aborted rows now store
  `detected_after_s`, and `ran_for_s` is None unless the dead pass itself
  wrote progress. Budget calibration never read these rows.

## Provenance

`analysis/daily_2026-09-22/`: `PRECOMMIT.md`, `s1_recheck.py` →
`s1_results.json`, `s1_realizable_pools.json`, `s2_useful.py` →
`s2_results.json`, `s3_survival.py` → `s3_results.json`.
