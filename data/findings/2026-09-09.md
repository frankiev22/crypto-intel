# Findings 2026-09-09

## 05:32:14 UTC · horizon-drift · 1h

1h horizon checked at a median 17.71x its label (17.71h elapsed) across 169 rows

```
0 rows were deferred to a later pass. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 05:34:45 UTC · horizon-drift · 6h

6h horizon checked at a median 2.96x its label (17.75h elapsed) across 120 rows

```
494 rows were deferred to a later pass. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 10:14:55 UTC · horizon-drift · 1h

1h horizon checked at a median 4.80x its label (4.80h elapsed) across 74 rows

```
0 rows were deferred to a later pass. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 10:16:03 UTC · horizon-drift · 6h

6h horizon checked at a median 3.86x its label (23.16h elapsed) across 49 rows

```
0 rows were deferred to a later pass. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 14:53:35 UTC · horizon-drift · 1h

1h horizon checked at a median 4.68x its label (4.68h elapsed) across 65 rows

```
0 rows were deferred to a later pass. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 14:55:23 UTC · horizon-drift · 6h

6h horizon checked at a median 1.58x its label (9.47h elapsed) across 74 rows

```
0 rows were deferred to a later pass. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 14:58:28 UTC · lookup-outage · 24h

24h horizon: primary price source resolved only 17/120 lookups (14%), under this horizon's 20% floor

```
103 of 120 lookups fell through to the fallback or failed entirely. Outcomes at this horizon are being priced off a fallback or not at all, which is how a scoring model dies quietly.
```

## 15:02:17 UTC · liveness · paper.open

paper.open has NEVER fired in the 51h since it was declared

```
A tracker that has never fired is broken or unnecessary, and which one it is has to be decided, not left. Expected: a paper position was opened. Threshold 48h (provisional).
```

## 18:06:17 UTC · horizon-drift · 1h

1h horizon checked at a median 3.24x its label (3.24h elapsed) across 74 rows

```
0 rows were deferred to a later pass. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 18:10:25 UTC · lookup-outage · 24h

24h horizon: primary price source resolved only 22/120 lookups (18%), under this horizon's 20% floor

```
98 of 120 lookups fell through to the fallback or failed entirely. Outcomes at this horizon are being priced off a fallback or not at all, which is how a scoring model dies quietly.
```

## 18:13:12 UTC · liveness · paper.open

paper.open has NEVER fired in the 54h since it was declared

```
A tracker that has never fired is broken or unnecessary, and which one it is has to be decided, not left. Expected: a paper position was opened. Threshold 48h (provisional).
```

## 21:21:30 UTC · horizon-drift · 1h

1h horizon checked at a median 3.28x its label (3.28h elapsed) across 66 rows

```
0 rows were deferred to a later pass. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 21:26:09 UTC · horizon-drift · 24h

24h horizon checked at a median 1.60x its label (38.30h elapsed) across 120 rows

```
145 rows were deferred to a later pass. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 21:26:09 UTC · lookup-outage · 24h

24h horizon: primary price source resolved only 17/120 lookups (14%), under this horizon's 20% floor

```
103 of 120 lookups fell through to the fallback or failed entirely. Outcomes at this horizon are being priced off a fallback or not at all, which is how a scoring model dies quietly.
```

## 21:29:14 UTC · lookup-outage · 168h

168h horizon: primary price source resolved only 1/120 lookups (1%), under this horizon's 1% floor

```
119 of 120 lookups fell through to the fallback or failed entirely. Outcomes at this horizon are being priced off a fallback or not at all, which is how a scoring model dies quietly.
```

## 21:29:16 UTC · liveness · paper.open

paper.open has NEVER fired in the 57h since it was declared

```
A tracker that has never fired is broken or unnecessary, and which one it is has to be decided, not left. Expected: a paper position was opened. Threshold 48h (provisional).
```

