# Findings 2026-09-18 (UTC)

All timestamps in this file, and the filename, are UTC. Count wins by CONTRACT ADDRESS, not by entry: one token pings once per horizon and each ping says which.

## 03:49:16 UTC · liveness · milestone.mcap

milestone.mcap last fired 70.6h ago, over its 48h threshold

```
Expected: a token crossed 100k/200k/1m/5m and was claimed LIVE. It has fired 1114 times before, so the path works and something stopped it.
```

## 03:49:17 UTC · liveness · milestone.realizable

milestone.realizable last fired 72.5h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 03:49:17 UTC · liveness · outcome.recorded

outcome.recorded last fired 70.5h ago, over its 12h threshold

```
Expected: an outcome row was written at some horizon. It has fired 28415 times before, so the path works and something stopped it.
```

## 03:49:17 UTC · liveness · paper.open

paper.open last fired 70.7h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 260 times before, so the path works and something stopped it.
```

## 03:49:18 UTC · liveness · paper.sweep

paper.sweep last fired 70.7h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 03:49:18 UTC · liveness · watchlist.sweep

watchlist.sweep last fired 70.6h ago, over its 12h threshold

```
Expected: the approach band was re-checked. It has fired 38 times before, so the path works and something stopped it.
```

## 03:53:04 UTC · liveness · milestone.mcap

milestone.mcap last fired 70.7h ago, over its 48h threshold

```
Expected: a token crossed 100k/200k/1m/5m and was claimed LIVE. It has fired 1114 times before, so the path works and something stopped it.
```

## 03:53:04 UTC · liveness · milestone.realizable

milestone.realizable last fired 72.6h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 03:53:04 UTC · liveness · outcome.recorded

outcome.recorded last fired 70.6h ago, over its 12h threshold

```
Expected: an outcome row was written at some horizon. It has fired 28415 times before, so the path works and something stopped it.
```

## 03:53:05 UTC · liveness · paper.open

paper.open last fired 70.8h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 264 times before, so the path works and something stopped it.
```

## 03:53:05 UTC · liveness · paper.sweep

paper.sweep last fired 70.7h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 03:53:05 UTC · liveness · watchlist.sweep

watchlist.sweep last fired 70.7h ago, over its 12h threshold

```
Expected: the approach band was re-checked. It has fired 38 times before, so the path works and something stopped it.
```

## 03:57:43 UTC · collector-error · launcher-restore-test

deliberate test failure to confirm alerting works from Task Scheduler

```
Raised on purpose 2026-09-18 while verifying the new launcher. The old path could not alert because there was no interpreter to run findings.py.
```

## 03:58:18 UTC · liveness · milestone.mcap

milestone.mcap last fired 70.8h ago, over its 48h threshold

```
Expected: a token crossed 100k/200k/1m/5m and was claimed LIVE. It has fired 1114 times before, so the path works and something stopped it.
```

## 03:58:18 UTC · liveness · milestone.realizable

milestone.realizable last fired 72.7h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 03:58:18 UTC · liveness · outcome.recorded

outcome.recorded last fired 70.7h ago, over its 12h threshold

```
Expected: an outcome row was written at some horizon. It has fired 28415 times before, so the path works and something stopped it.
```

## 03:58:18 UTC · liveness · paper.open

paper.open last fired 70.9h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 264 times before, so the path works and something stopped it.
```

## 03:58:18 UTC · liveness · paper.sweep

paper.sweep last fired 70.8h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 03:58:18 UTC · liveness · watchlist.sweep

watchlist.sweep last fired 70.8h ago, over its 12h threshold

```
Expected: the approach band was re-checked. It has fired 38 times before, so the path works and something stopped it.
```

## 03:58:20 UTC · liveness · milestone.mcap

milestone.mcap last fired 70.8h ago, over its 48h threshold

```
Expected: a token crossed 100k/200k/1m/5m and was claimed LIVE. It has fired 1114 times before, so the path works and something stopped it.
```

## 03:58:20 UTC · liveness · milestone.realizable

milestone.realizable last fired 72.7h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 03:58:20 UTC · liveness · outcome.recorded

outcome.recorded last fired 70.7h ago, over its 12h threshold

```
Expected: an outcome row was written at some horizon. It has fired 28415 times before, so the path works and something stopped it.
```

## 03:58:20 UTC · liveness · paper.open

paper.open last fired 70.9h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 264 times before, so the path works and something stopped it.
```

## 03:58:20 UTC · liveness · paper.sweep

paper.sweep last fired 70.8h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 03:58:20 UTC · liveness · watchlist.sweep

watchlist.sweep last fired 70.8h ago, over its 12h threshold

```
Expected: the approach band was re-checked. It has fired 38 times before, so the path works and something stopped it.
```

## 03:58:30 UTC · liveness · milestone.mcap

milestone.mcap last fired 70.8h ago, over its 48h threshold

```
Expected: a token crossed 100k/200k/1m/5m and was claimed LIVE. It has fired 1114 times before, so the path works and something stopped it.
```

## 03:58:30 UTC · liveness · milestone.realizable

milestone.realizable last fired 72.7h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 03:58:30 UTC · liveness · outcome.recorded

outcome.recorded last fired 70.7h ago, over its 12h threshold

```
Expected: an outcome row was written at some horizon. It has fired 28415 times before, so the path works and something stopped it.
```

## 03:58:30 UTC · liveness · paper.open

paper.open last fired 70.9h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 264 times before, so the path works and something stopped it.
```

## 03:58:30 UTC · liveness · paper.sweep

paper.sweep last fired 70.8h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 04:00:19 UTC · horizon-drift · 1h

1h horizon checked at a median 8.53x its label (8.53h elapsed) across 59 rows

```
0 rows were not reached this pass; a row still unscored 18h after coming due ages out unscored. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 04:00:21 UTC · liveness · milestone.realizable

milestone.realizable last fired 72.7h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 04:00:21 UTC · liveness · paper.open

paper.open last fired 70.9h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 264 times before, so the path works and something stopped it.
```

## 04:00:21 UTC · liveness · paper.sweep

paper.sweep last fired 70.9h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 04:02:11 UTC · liveness · milestone.realizable

milestone.realizable last fired 72.7h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 04:02:11 UTC · liveness · paper.open

paper.open last fired 70.9h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 264 times before, so the path works and something stopped it.
```

## 04:02:11 UTC · liveness · paper.sweep

paper.sweep last fired 70.9h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 04:02:20 UTC · liveness · milestone.realizable

milestone.realizable last fired 72.7h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 04:02:20 UTC · liveness · paper.open

paper.open last fired 70.9h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 264 times before, so the path works and something stopped it.
```

## 04:02:20 UTC · liveness · paper.sweep

paper.sweep last fired 70.9h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 04:04:10 UTC · liveness · milestone.realizable

milestone.realizable last fired 72.8h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 04:04:10 UTC · liveness · paper.open

paper.open last fired 70.9h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 264 times before, so the path works and something stopped it.
```

## 04:04:10 UTC · liveness · paper.sweep

paper.sweep last fired 70.9h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 11:51:49 UTC · horizon-drift · 1h

1h horizon checked at a median 7.95x its label (7.95h elapsed) across 200 rows

```
10 rows were not reached this pass; a row still unscored 18h after coming due ages out unscored. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 11:54:28 UTC · liveness · milestone.realizable

milestone.realizable last fired 80.6h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 11:54:28 UTC · liveness · paper.close

paper.close last fired 78.8h ago, over its 72h threshold

```
Expected: a paper position was closed - the losers, the evidence. It has fired 143 times before, so the path works and something stopped it.
```

## 11:54:28 UTC · liveness · paper.open

paper.open last fired 78.8h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 264 times before, so the path works and something stopped it.
```

## 11:54:28 UTC · liveness · paper.sweep

paper.sweep last fired 78.8h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 12:09:23 UTC · horizon-drift · 1h

1h horizon checked at a median 16.71x its label (16.71h elapsed) across 10 rows

```
0 rows were not reached this pass; a row still unscored 18h after coming due ages out unscored. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 12:11:18 UTC · liveness · milestone.realizable

milestone.realizable last fired 80.9h ago, over its 48h threshold

```
Expected: a realizable 2x/3x/10x was claimed. It has fired 48 times before, so the path works and something stopped it.
```

## 12:11:18 UTC · liveness · paper.close

paper.close last fired 79.0h ago, over its 72h threshold

```
Expected: a paper position was closed - the losers, the evidence. It has fired 143 times before, so the path works and something stopped it.
```

## 12:11:18 UTC · liveness · paper.open

paper.open last fired 79.1h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 272 times before, so the path works and something stopped it.
```

## 12:11:18 UTC · liveness · paper.sweep

paper.sweep last fired 79.0h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

## 12:40:58 UTC · selftest · k

summary

## 12:43:10 UTC · selftest · k

summary

## 13:56:01 UTC · selftest · k

summary

## 14:31:10 UTC · outcome-win · 7uMjiTCQiRaKMCrYKaNMQHR6h5Bo9qzd42ycTAWzRhE1

UNKNOWN 4.58x, realizable, at the 1h horizon (measured 2.73h after observation) [first horizon to clear for this contract]

```
contract 7uMjiTCQiRaKMCrYKaNMQHR6h5Bo9qzd42ycTAWzRhE1
pair     BqkkGUitSRdpT3HoVg3xR1P94DXnPhDqwR7a29CEEGp1
entry    $6.588e-06
now      $3.018e-05
exit depth $12,807 of $26,337 reported
price     confirmed, sources within 1.0007x
NOMINAL 1h, ACTUAL 2.73h elapsed - read actual_elapsed_h, not the label.
```

## 14:32:30 UTC · horizon-drift · 1h

1h horizon checked at a median 2.73x its label (2.73h elapsed) across 141 rows

```
0 rows were not reached this pass; a row still unscored 18h after coming due ages out unscored. Nominal horizon_h is a label, not a measurement - read actual_elapsed_h instead. Anything bucketed on the label is measuring a variable window.
```

## 15:40:40 UTC · liveness · paper.close

paper.close last fired 82.5h ago, over its 72h threshold

```
Expected: a paper position was closed - the losers, the evidence. It has fired 143 times before, so the path works and something stopped it.
```

## 15:40:40 UTC · liveness · paper.open

paper.open last fired 82.6h ago, over its 48h threshold

```
Expected: a paper position was opened. It has fired 307 times before, so the path works and something stopped it.
```

## 15:40:41 UTC · liveness · paper.sweep

paper.sweep last fired 82.5h ago, over its 12h threshold

```
Expected: the paper log looked for positions to close. It has fired 39 times before, so the path works and something stopped it.
```

