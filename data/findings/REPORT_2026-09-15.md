# Input 54: the staged list, the budget, the grade (2026-09-15)

Figures as of 05:18Z. Commits: 57e4608 (P0/P1), 0b7767d (P2, scan deadline, dashboard). All ten test suites pass (260 checks).

## Corrections first

1. **The dashboard's "Graduated" section has been showing dead pools as graduations.** 21 of the 31 graduation claims on record are FDV prints with no real pool behind them, including 17 of the 24 from the last seven days. Most of those pools held under $1. The first claim made by the fixed, unattended watchlist was one of them: Minecraft (`4cUCNc1q…zNTG`), FDV $135,771 on $0.49 of liquidity, confirmed on both sources. **Fixed at the surface in 0b7767d**: only claims with a real pool are shown as graduated, and the others are counted beside them as not graduated.

2. **The n=200 date was wrong.** "~9/16–17", and my own "9/17 at the full-collection rate" on 9/14, both assumed an unattended sweep that did not exist on the host. The honest answer is in P0.2 below.

3. **v1's rate rose from 9.46% to 11.84% overnight on two wins whose pools were drained within ten minutes.** Oiled (2.918x) and titcoin (3.353x) each closed 2m49s after entry. Nine minutes later, both pools held under $1. Without the three sub-3-minute targets (OPAI, Oiled, titcoin), v1 measured is **8.22%**. Both versions are in the figures.

4. **Five premises in the brief did not survive re-derivation:**
   - **Budget:** it has not assumed 1.116s since 9/10. It is a deadline with per-call cost measured as it runs. The kill came from `scanner.scan` obeying only its own 150s constant (P1).
   - **"SIGKILLed at 214s":** 214s is the time until the *next* command started, so it is an upper bound. The kill is at ~178s.
   - **"Seventeen prior runs":** kills recorded since 9/10 are 19 on 9/10 (9 of them at stage 1), 2 on 9/11, and 3 on 9/15 (2 scan, 1 full). There is no record of seventeen scan kills. No finding mentions `--max-seconds 110`, and the memory writer's own 9/14 file calls it a first, in-session workaround.
   - **`milestone.mcap` / `milestone.realizable` were being invoked** by stages 1/6/24. `mcap` fired 4 times under `scheduled` origin in the 05:05Z run; their staleness was host downtime. **`milestone.graduated` was in the trap, though, and the brief did not list it**, because only `watchlist.sweep` can fire it.
   - **"Li Pump 4.74x … inside an hour":** 4.738x on $24,554 of exit depth against $49,523 reported is correct, but the measured gap was **1.20h**, not under an hour. The *paper* close of Li Pump (v2, 3.874x) was made by the ad-hoc, killed unstaged pass at 03:13Z, not by the system.
   - `watchlist.sweep` at 4x its limit was true when written.

## P0: the five components

**1. Whose closes are the reported counts?** Mostly the system's.

| origin | v1 closes | v2 closes |
|---|---|---|
| GitHub runner (unattended) | 83 | 207 |
| host unstaged full pass, to 9/10 (whether anyone was watching is unknown) | 81 | 0 |
| my 9/14 hand sweep | 16 | 66 |
| ad-hoc killed unstaged pass, 9/15 03:12Z | 0 | 1 |
| **host `--stage sweep`, unattended, 9/15 05:08Z** | **2** | **3** |
| total | 182 | 277 |

Nothing unattended closed a position between 9/12 19:44Z, when the runner was blocked by the billing hold, and 05:08Z today.

**2. n=200, recomputed against what actually runs.** v1 has 143 closed and 16 open (those fall due by ~9/16 05Z), so it needs 57 more closes. At the host's measured 1.5 v1 entries per scanned hour:
- **At best, ~Thursday 9/17 morning UTC.** That requires the host to stay up every hour and the new sweep stage to keep working.
- **At post-epoch coverage (29% of hours had a scan), 9/19–9/20.** That is after the usual Friday ~16:00Z shutdown, so in practice the **week of 9/21**.
- **Never, while nothing sweeps.**

**3. How long each was absent, and its state now.**

| component | reachable from staged list before 9/15 | last unattended beat before the fix | 05:05Z unattended run |
|---|---|---|---|
| `paper.sweep` | never | 9/12 ~19:44Z (runner) | ✅ 05:08:30Z |
| `paper.close` | never | 9/12 19:44Z (runner) | ✅ 05:08:50Z, 05:08:53Z (2 targets) |
| `watchlist.sweep` | never | 9/12 (runner) | ✅ 05:10:52Z |
| `milestone.graduated` (not in brief) | never | 9/12 (runner) | ✅ 05:10:55Z (the dead-pool claim above) |
| `milestone.mcap` | yes (stages 1/6/24) | 9/15 04:09Z | ✅ ×4 |
| `milestone.realizable` | yes (same `check_outcome` call) | 9/15 03:18Z | not fired this hour; it needs a realizable 2x+ outcome |

"Never" means since the paper log and watchlist were written (9/06–9/07): both were wired only into `one_pass()`, the unstaged full pass. State now: v1 16 open, v2 76 open. The 05:10Z sweep deferred 5 positions to the next hour, as designed.

**The liveness question: it fired, it escalated, and it was ignored, for reasons.** `watchlist.sweep` produced 91 stale findings and 4 escalations; `paper.sweep` produced 55 and 3.
- **It cried wolf first.** All 104 stale findings on 9/10–9/11 were *false*: the runner had swept, but its beats only reach this machine when its commits are merged.
- **Manual runs silenced the real alarm.** My 9/14 hand sweep, and the killed ad-hoc pass at 03:12Z, reset both components to ok while the staged list still called neither.

**Fixed:** beats now record their origin, and staleness counts only unattended beats. **Not fixed:** a component that only the runner exercises is still blind to unmerged runner commits.

**Fix, verified by observation.** `--stage sweep` and `--stage watchlist` now exist. The list is code (`collect.STAGES`), and `test_stages.py` fails if any liveness component is unreachable or the skill file drifts from it. Unknown stage names are refused instead of silently running a killable full pass. The first unattended run (05:05Z), every stage with `origin=scheduled`:

| scan | sweep | watchlist | 1h | 6h | 24h | 168h |
|---|---|---|---|---|---|---|
| 164s / 110 (overran, see P1) | 138s, 5 deferred | 9s | 112s, 45 of 179 pairs | 111s | 13s | 110s |

None was killed.

**Its two target closes, audited.** Oiled and titcoin were real prints on $2,973 and $2,000 of depth, and both pools were drained to under $1 nine minutes later: quotes, not fills (GAPS.md). The v2 close LELD (B_low arm, 9.17x on $22,332) is genuine: both sources are above 14x now, on $32k–$59k of liquidity. v1 and v2 closed on the same price read.

## P1: the budget

- `--max-seconds 110` is on all seven skill-file lines, as a ceiling.
- **The budget calibrates itself.** Each stage's deadline is min(ceiling, 178s − 1.25 × that stage's worst recorded overrun), with a 30s floor. It re-derives every run.
- **Root cause, exposed by the first unattended pass.** `scanner.scan` stopped only at its own `SCAN_BUDGET_S = 150` and ignored the stage deadline, so the scan ran 164s under a 110s budget. **Fixed in 0b7767d:** it stops when the time left is less than 1.5 × the measured p75 cost of a row. The first unattended check of this is the 06:05Z run.
- **What 110 costs.** The 1h stage reached 45 of 179 due pairs, against 90 of 152 an hour earlier at 155s. The outcome stages overrun by ≤2s, so calibration alone would give them 163s. **Recommendation:** keep 110 on scan and sweep only. I have not changed it, because you asked for 110.

## P2: score 100 with live authorities

- **`score` is unchanged.** v1's pinned gate and v2's arm split both read it, so rewriting it mid-run would be the substitution bug again. Every human-facing surface reads **`grade`** instead:
  - a live authority: grade **0**, shown as TRAP
  - an unverified authority: min(score, **69**), so it can never pass the 70 filter
  - both revoked: the score
- The rule was pre-committed in 57e4608, before anything was measured (PRECOMMIT_surface_grade.md).
- **Replay over 10,469 post-epoch observations:** 0 of 137 v1 and 0 of 709 v2 entry decisions change.
- **Effect:** 58 of 684 score-70+ rows no longer surface. Two had a live authority and 56 were unverified; 49 of those are rows from before 9/10, when no skip reason was recorded.
- **The two post-epoch 100s with mint AND freeze authority live:** QUBT (9/09) and PTN (9/14). Both are now grade 0.
- **Surfaces covered:** the Discord alert list and its title, the scanner's output, and the skill file's scanner-hit rule (now "grade"). Traps are keyed on contract address and never shown with a score. The first unattended pass using this is 06:05Z.

## P3

- **9/11 and 9/12 daily summaries are backfilled** from telemetry and marked partial, with their sources listed (`dispatch-workspace/memory/daily-summaries/`).
- **168h: both readings were true.** From 9/14 18Z to 9/15 ~05Z, 1,120 rows arrived, 771 were scored and 1,618 aged out unscored, taking the queue from 2,095 to 826. The queue shrinks by expiry, not by work. `track.py` declares 168h retired, but `--stage 168` bypasses that, spending ~100 calls an hour on a horizon that resolves 2.7–5.1% of the time. The collector's wording is fixed. **Recommend removing the stage.** It is a Claude-side stage, so I have left it in place.

## Figures, both ways

**v1:** 159 entries, 143 closed, 16 open. `gate_drift()` none.

| | all | late closes excluded | 3-minute targets excluded |
|---|---|---|---|
| measured | 9/76 = 11.84% [6.36, 21.00] | 9/74 = 12.16% [6.53, 21.53] | 6/73 = 8.22% [3.82, 16.79] |
| + inferred losses | 9/123 = 7.32% [3.90, 13.32] | 9/110 = 8.18% [4.36, 14.82] | 6/120 = 5.00% [2.31, 10.48] |

Paper P&L on $100 clips: **−$3,099** over 76 measured closes (−$3,930 with wins capped at 2x), and **−$7,799** over 123 with inferred losses (−$8,630 capped).

**v2:** 353 entries, 277 closed, 76 open. `gate_drift()` none.

| arm | entered / closed tokens | hit rate | late excluded |
|---|---|---|---|
| A | 72 / 29 | withheld (one short of 30) | withheld |
| B_high | 191 / 58 | 6/58 = 10.34% [4.83, 20.79] | 6/49 = 12.24% [5.73, 24.24] |
| B_low | 90 / 13 | withheld | withheld |

**Still not proven:** the paper log loses money on every cut. **Still Frank's to do:** clear the GitHub billing hold (every run refused since 9/12 21:43Z). While the host is off, the runner is the only collector.
